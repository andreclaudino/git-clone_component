from setuptools import setup, find_packages

setup(
    name='git-clone_component',
    version='1.0.0',
    packages=find_packages(),
    url='',
    license='',
    author='',
    author_email='',
    description='A component for cloning git repositories',
    install_requires=[
        "kfp~=1.8.12"
    ]
)